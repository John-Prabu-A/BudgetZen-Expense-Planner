# 🏗️ BudgetZen Notification System - Architecture Diagrams

**Visual Reference Guide**  
**Updated:** December 21, 2025

---

## 1️⃣ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER DEVICES                                 │
│                    (iOS / Android)                                   │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  BudgetZen Mobile App (Expo)                                 │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  Notification Handler                                  │  │  │
│  │  │  - Display notifications                              │  │  │
│  │  │  - Handle user taps                                   │  │  │
│  │  │  - Track delivery                                     │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  Push Token Manager                                    │  │  │
│  │  │  - Register device                                    │  │  │
│  │  │  - Get token                                          │  │  │
│  │  │  - Handle refresh                                     │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  Job Scheduler                                         │  │  │
│  │  │  - Start on app launch                               │  │  │
│  │  │  - Run background jobs                               │  │  │
│  │  │  - Check for timers                                  │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  Queue Manager                                         │  │  │
│  │  │  - Listen for new notifications                       │  │  │
│  │  │  - Send pending notifications                         │  │  │
│  │  │  - Track queue status                                 │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                   HTTPS (Secure, Encrypted)
                              │
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌──────────────────────┐              ┌──────────────────────┐
│  Supabase (Backend)  │              │  Supabase (Realtime) │
└──────────────────────┘              └──────────────────────┘
        │                                           │
        │                                    Subscribe to:
        │                                    - notification_queue
        │                                    - job_logs
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│              PostgreSQL Database                             │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ push_tokens          │  │ notification_queue   │        │
│  │ - token              │  │ - user_id            │        │
│  │ - platform           │  │ - title              │        │
│  │ - is_valid           │  │ - body               │        │
│  │ - last_verified_at   │  │ - status             │        │
│  └──────────────────────┘  └──────────────────────┘        │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ notification_log     │  │ notification_prefs   │        │
│  │ - user_id            │  │ - timezone           │        │
│  │ - notification_type  │  │ - dnd_enabled        │        │
│  │ - status             │  │ - daily_reminder_*   │        │
│  │ - delivered_at       │  │ - weekly_summary_*   │        │
│  │ - opened_at          │  │ - etc.               │        │
│  └──────────────────────┘  └──────────────────────┘        │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ job_execution_logs   │  │ notification_throttle│        │
│  │ - job_name           │  │ - user_id            │        │
│  │ - executed_at        │  │ - notification_type  │        │
│  │ - success            │  │ - last_sent_at       │        │
│  │ - error_message      │  │ - count_today        │        │
│  └──────────────────────┘  └──────────────────────┘        │
└──────────────────────────────────────────────────────────────┘
        │
        │  RPC Calls: queue_notification(), is_in_dnd_hours()
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│          Supabase Edge Functions (Serverless)                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ send-notification                                    │  │
│  │ - Get user's push tokens                             │  │
│  │ - Validate tokens                                    │  │
│  │ - Send via Expo Push API                            │  │
│  │ - Log delivery status                               │  │
│  │ - Mark invalid tokens                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ schedule-daily-jobs                                  │  │
│  │ - Get users with preference enabled                 │  │
│  │ - Check timezone and scheduled time                 │  │
│  │ - Respect DND hours                                 │  │
│  │ - Queue notifications for each user                 │  │
│  │ - Log job execution                                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ process-queue                                        │  │
│  │ - Get pending notifications                         │  │
│  │ - Call send-notification for each                   │  │
│  │ - Implement retry with backoff                      │  │
│  │ - Update queue status                               │  │
│  │ - Log failures                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ schedule-weekly-jobs                                 │  │
│  │ - Weekly summary processor                          │  │
│  │ - Compliance report generator                       │  │
│  │ - Trend analysis                                    │  │
│  └──────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
        │
        │  HTTPS API Calls
        │
        ▼
┌──────────────────────────────────────────────────────────────┐
│              Expo Push Service                               │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │ APNs                 │  │ FCM                  │        │
│  │ (Apple Push)         │  │ (Firebase Cloud)     │        │
│  │                      │  │                      │        │
│  │ ▼ ▼ ▼              │  │ ▼ ▼ ▼              │        │
│  │ iOS Devices          │  │ Android Devices      │        │
│  └──────────────────────┘  └──────────────────────┘        │
└──────────────────────────────────────────────────────────────┘
```

---

## 2️⃣ Data Flow: Real-Time Alert

```
Timeline: Immediate (< 1 second)

TIME 0ms
┌──────────────────────────────────────────────┐
│ User adds expense: $50                       │
│ Amount: $50                                  │
│ Category: Food                               │
│ Budget for Food: $100                        │
│ Current Spent: $90                           │
│ Total after: $140 (140% of budget)           │
└──────────────────────────────────────────────┘
         │
         │ INSERT into records table
         ▼
TIME 50ms
┌──────────────────────────────────────────────┐
│ Database Trigger Fires                       │
│ (or Edge Function monitors)                  │
│                                              │
│ CHECK: Is spending > 100% of budget?         │
│ YES! $140 > $100                             │
└──────────────────────────────────────────────┘
         │
         │ INSERT into notification_queue
         ▼
TIME 100ms
┌──────────────────────────────────────────────┐
│ Notification Queued                          │
│ Type: budget_exceeded                        │
│ Title: "Budget Alert: Food"                  │
│ Body: "You've exceeded your budget"          │
│ Status: pending                              │
│ Scheduled: NOW                               │
└──────────────────────────────────────────────┘
         │
         │ Edge Function triggered
         ▼
TIME 150ms
┌──────────────────────────────────────────────┐
│ send-notification Edge Function               │
│ 1. Get push tokens for user                  │
│ 2. For each token:                           │
│    - Check if token is valid                 │
│    - Call Expo Push API                      │
│    - Log delivery                            │
│ 3. Mark failed tokens as invalid             │
└──────────────────────────────────────────────┘
         │
         │ HTTPS to Expo API
         ▼
TIME 300ms
┌──────────────────────────────────────────────┐
│ Expo Push Service                            │
│ Receives push notification request           │
│ Routes to appropriate service:               │
│ - iOS → APNs                                 │
│ - Android → FCM                              │
└──────────────────────────────────────────────┘
         │
         │ APNs/FCM deliver to device
         ▼
TIME 500-1000ms
┌──────────────────────────────────────────────┐
│ User Device Receives Notification            │
│ ✓ Notification shows on lock screen          │
│ ✓ System plays sound                         │
│ ✓ Badge updates                              │
└──────────────────────────────────────────────┘
         │
         │ User sees & taps notification
         ▼
TIME 2000ms+
┌──────────────────────────────────────────────┐
│ App Opens to Budget Screen                   │
│ User sees budget details                     │
│ Can adjust spending or budget                │
└──────────────────────────────────────────────┘

TOTAL LATENCY: < 1.5 seconds from action to notification
```

---

## 3️⃣ Data Flow: Scheduled Job

```
Timeline: Scheduled (daily/weekly)

TIME: Daily at 7 PM UTC

┌────────────────────────────────────────────────────┐
│ CRON Timer Triggers                                │
│ schedule-daily-jobs Edge Function                  │
└────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────┐
│ Fetch All Active Users (100 users)                 │
│                                                    │
│ Query: notification_preferences                    │
│   WHERE daily_reminder_enabled = true              │
│                                                    │
│ Returns: 100 user IDs with preferences             │
└────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────┐
│ For Each User (Batch Processing)                   │
│                                                    │
│ 1. Get user timezone (e.g., 'America/New_York')   │
│ 2. Get preferred time (e.g., '19:00')             │
│ 3. Calculate if it's the scheduled time           │
│ 4. Check if user is in DND hours                  │
│ 5. If all checks pass → Queue notification        │
│                                                    │
│ Processing: ~200ms per user                       │
│ Total time: 100 users × 200ms = 20 seconds       │
└────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────┐
│ Insert into notification_queue                     │
│                                                    │
│ Example rows:                                      │
│ ┌──────────────────────────────────────────────┐  │
│ │ user_id: abc123                              │  │
│ │ title: "📝 Log today's expenses"             │  │
│ │ body: "Ready to track your spending?"        │  │
│ │ status: 'pending'                            │  │
│ │ scheduled_for: NOW                           │  │
│ │ idempotency_key: reminder_abc123_2025-12-21 │  │
│ └──────────────────────────────────────────────┘  │
│                                                    │
│ Inserted: 85 notifications (15 skipped - DND)    │
└────────────────────────────────────────────────────┘
         │
         │ Time: 7:05 PM UTC
         ▼
┌────────────────────────────────────────────────────┐
│ Job Execution Logged                               │
│                                                    │
│ INSERT into job_execution_logs:                    │
│ - job_name: 'daily_reminder'                       │
│ - executed_at: NOW                                 │
│ - success: true                                    │
│ - notifications_sent: 85                           │
│ - notifications_failed: 0                          │
│ - duration_ms: 25000                               │
└────────────────────────────────────────────────────┘
         │
         │ process-queue runs (every 5 min)
         ▼
┌────────────────────────────────────────────────────┐
│ Process Notification Queue                         │
│                                                    │
│ 1. Query pending notifications                    │
│    WHERE status = 'pending'                        │
│    AND scheduled_for <= NOW                        │
│    LIMIT 20 (batch size)                           │
│                                                    │
│ 2. For each notification:                          │
│    - Call send-notification                       │
│    - Get user's push tokens                       │
│    - Send via Expo API                            │
│    - Update status                                │
│                                                    │
│ 3. Repeat until queue empty                        │
│                                                    │
│ Result: 85 notifications sent                     │
└────────────────────────────────────────────────────┘
         │
         │ Over next 2 hours
         ▼
┌────────────────────────────────────────────────────┐
│ Users Receive Notifications                        │
│                                                    │
│ User in New York (7 PM EST):                       │
│ • Receives: "Log today's expenses"                │
│ • At their preferred time                         │
│ • Respects DND if set                             │
│                                                    │
│ User in London (12 AM GMT next day):              │
│ • Receives: Same notification                     │
│ • Also at their preferred time (7 PM GMT)         │
│ • Respects their local DND                        │
└────────────────────────────────────────────────────┘

TOTAL LATENCY: 5 minutes (from 7 PM to all sent by 7:05 PM)
```

---

## 4️⃣ Queue Processing Flow

```
PENDING NOTIFICATIONS

Time: 7:05 PM (Right after schedule-daily-jobs)

notification_queue table:
┌─────┬──────────────────────┬─────────────────┬────────────┐
│ ID  │ User ID              │ Title           │ Status     │
├─────┼──────────────────────┼─────────────────┼────────────┤
│ 1   │ user123              │ Log expenses    │ pending    │
│ 2   │ user456              │ Log expenses    │ pending    │
│ 3   │ user789              │ Log expenses    │ pending    │
│ ... │ ...                  │ ...             │ pending    │
│ 85  │ user_xyz             │ Log expenses    │ pending    │
└─────┴──────────────────────┴─────────────────┴────────────┘

Every 5 minutes, process-queue runs:

Step 1: Query pending (batch of 20)
┌────────────────────────────────────────────┐
│ SELECT * FROM notification_queue           │
│ WHERE status = 'pending'                   │
│ ORDER BY scheduled_for ASC                 │
│ LIMIT 20                                   │
│                                            │
│ Returns: IDs 1, 2, 3, ..., 20              │
└────────────────────────────────────────────┘

Step 2: Send notifications
┌────────────────────────────────────────────┐
│ For notification 1 (user123):               │
│                                            │
│ 1. Get push tokens for user123             │
│    (iOS: token_ios, Android: token_android)│
│                                            │
│ 2. Call send-notification function         │
│    with tokens and message                 │
│                                            │
│ 3. Receive response:                       │
│    SUCCESS: {                              │
│      "success": true,                      │
│      "successCount": 2,                    │
│      "expoIds": ["id1", "id2"]             │
│    }                                       │
│                                            │
│ 4. Update notification_queue:              │
│    UPDATE SET status = 'sent'               │
│                                            │
│ 5. Insert into notification_log:           │
│    (for tracking/analytics)                │
│                                            │
│ Repeat for notifications 2-20              │
└────────────────────────────────────────────┘

Step 3: Handle failures (if any)
┌────────────────────────────────────────────┐
│ If send failed for notification 21:        │
│                                            │
│ 1. Check: attempts < max_attempts?         │
│    YES (1 < 3)                             │
│                                            │
│ 2. Schedule retry:                         │
│    next_attempt = NOW + 2^1 minutes        │
│    = NOW + 2 minutes                       │
│                                            │
│ 3. Update notification_queue:              │
│    UPDATE SET                              │
│      status = 'pending'                    │
│      attempts = 2                          │
│      scheduled_for = next_attempt          │
│                                            │
│ 4. Will retry at 7:07 PM                   │
└────────────────────────────────────────────┘

Step 4: Continue until queue empty
┌────────────────────────────────────────────┐
│ 7:05 PM: Process 20 (ID 1-20)              │
│ 7:10 PM: Process 20 (ID 21-40)             │
│ 7:15 PM: Process 20 (ID 41-60)             │
│ 7:20 PM: Process 20 (ID 61-85)             │
│ 7:25 PM: Queue empty, all sent!            │
└────────────────────────────────────────────┘

Result: 85 notifications delivered
Status: All marked as 'sent' in notification_log
Logs: Full audit trail in notification_log table
```

---

## 5️⃣ Retry Logic (Exponential Backoff)

```
Failed Notification Retry Schedule

Notification queued at: 7:00 PM
Status: pending

ATTEMPT 1 (Immediate)
─────────────────────
Time: 7:00 PM
Status: Try to send
Result: FAILED (network error)

│ Attempt count: 0
│ Backoff calculation: 2^0 = 1 minute
│
▼

ATTEMPT 2
─────────
Time: 7:01 PM (1 minute later)
Status: pending
Attempts: 1/3
Result: FAILED (token invalid)

│ Attempt count: 1
│ Backoff calculation: 2^1 = 2 minutes
│
▼

ATTEMPT 3
─────────
Time: 7:03 PM (2 minutes later)
Status: pending
Attempts: 2/3
Result: SUCCESS ✓

│ Mark as 'sent'
│ Log delivery
│
▼

FINAL STATUS
────────────
notification_queue:
- status: 'sent'
- attempts: 3
- last_attempt_at: 7:03 PM

notification_log:
- status: 'delivered'
- sent_at: 7:00 PM
- delivered_at: 7:03 PM
- response_time_ms: 180000

---

What if all attempts fail?

ATTEMPT 1: 7:00 PM - FAILED
ATTEMPT 2: 7:02 PM - FAILED  (1 + 2 = 3 min later)
ATTEMPT 3: 7:06 PM - FAILED  (3 + 4 = 7 min later)
         ↓
Max attempts exceeded (3/3)
         ↓
Status: 'failed'
Manual review needed
```

---

## 6️⃣ DND Hours Check

```
User Settings:
- timezone: 'America/New_York'
- dnd_enabled: true
- dnd_start_time: '22:00'  (10 PM)
- dnd_end_time: '08:00'     (8 AM)

Timeline Example:

6:00 AM EST ─ Not in DND ✓
  Notification type: budget_exceeded
  Critical? YES (ignore DND)
  Send? YES

8:01 AM EST ─ Not in DND ✓
  Notification type: daily_reminder
  Critical? NO (respect DND)
  Send? YES (DND ended at 8 AM)

10:00 PM EST ─ IN DND ✗
  Notification type: daily_reminder
  Critical? NO
  Send? NO (Wait until 8 AM)

2:00 AM EST ─ IN DND ✗
  Notification type: achievement
  Critical? NO
  Send? NO (Still in DND)

8:00 AM EST ─ Not in DND ✓
  Notification type: budget_warning
  Critical? NO
  Send? YES (DND period ended)

─────────────────────────────────

DND Hours in Database:

notification_preferences:
┌──────────────────────────────┐
│ user_id: user123             │
│ timezone: America/New_York   │
│ dnd_enabled: true            │
│ dnd_start_time: 22:00        │
│ dnd_end_time: 08:00          │
└──────────────────────────────┘

Check Function Call:
SELECT is_in_dnd_hours('user123', NOW());

Returns: true or false
```

---

## 7️⃣ Timezone-Aware Scheduling

```
Same Notification, Different Times

Job: Daily Reminder at "19:00"

┌─────────────────────────────────────────────────┐
│ USER A: timezone = 'America/New_York'           │
│ Scheduled time: 19:00                           │
│ → Sends at 7:00 PM EST                          │
└─────────────────────────────────────────────────┘
         │
         │ (Same UTC time? NO!)
         │
         ▼
┌─────────────────────────────────────────────────┐
│ USER B: timezone = 'Europe/London'              │
│ Scheduled time: 19:00                           │
│ → Sends at 7:00 PM GMT                          │
└─────────────────────────────────────────────────┘
         │
         │ (Same UTC time? NO!)
         │
         ▼
┌─────────────────────────────────────────────────┐
│ USER C: timezone = 'Asia/Tokyo'                 │
│ Scheduled time: 19:00                           │
│ → Sends at 7:00 PM JST                          │
└─────────────────────────────────────────────────┘

─────────────────────────────────────────────────

When Converted to UTC:

User A: 7 PM EST = 12 AM UTC (next day)
User B: 7 PM GMT = 7 PM UTC (same day)
User C: 7 PM JST = 10 AM UTC (same day)

Result:
Each user gets notification at their 7 PM local time,
not all at the same UTC time!
```

---

## 8️⃣ Notification Status Lifecycle

```
┌─────────────┐
│   PENDING   │
│ - Just      │
│   queued    │
│ - Waiting   │
│   to send   │
└─────────────┘
      │
      │ process-queue sends
      │
      ▼
┌─────────────┐
│    SENT     │
│ - Sent to   │
│   Expo API  │
│ - Waiting   │
│   delivery  │
└─────────────┘
      │
      │ If delivery confirmation received
      │
      ▼
┌─────────────┐
│ DELIVERED   │
│ - Reached   │
│   device    │
│ - Might not │
│   be opened │
└─────────────┘
      │
      │ If user taps
      │
      ▼
┌─────────────┐
│   OPENED    │
│ - User saw  │
│ - User took │
│   action    │
└─────────────┘

─────────────────────────────

Failure Path:

┌─────────────┐
│   PENDING   │
└─────────────┘
      │
      │ send fails + retries < max
      │
      ▼
┌─────────────┐
│   PENDING   │ (retry scheduled)
│ attempts++  │
└─────────────┘
      │
      │ (repeat)
      │
      ▼
┌─────────────┐
│   FAILED    │
│ - Max       │
│   retries   │
│   exceeded  │
│ - Manual    │
│   review    │
│   needed    │
└─────────────┘

─────────────────────────────

Other outcomes:

BOUNCED: User unsubscribed / token invalid
DISCARDED: User deleted / system cleanup
```

---

## 9️⃣ Idempotency Key Prevention of Duplicates

```
Scenario: Bug causes same notification queued twice

Time: 7:00 PM
─────────────

First Queue Attempt:
┌──────────────────────────────────┐
│ rpc('queue_notification', {       │
│   p_user_id: 'user123',          │
│   p_notification_type: 'reminder',│
│   ...                            │
│   p_idempotency_key:             │
│     'reminder_user123_2025-12-21'│
│ })                               │
└──────────────────────────────────┘
         │
         ▼
     Database checks:
     WHERE idempotency_key = '...'
     NOT FOUND
         │
         ▼
     INSERT new record
     ✓ Created

Second Queue Attempt (Bug!):
┌──────────────────────────────────┐
│ rpc('queue_notification', {       │
│   p_user_id: 'user123',          │
│   p_notification_type: 'reminder',│
│   ...                            │
│   p_idempotency_key:             │
│     'reminder_user123_2025-12-21'│ (SAME KEY)
│ })                               │
└──────────────────────────────────┘
         │
         ▼
     Database checks:
     WHERE idempotency_key = '...'
     FOUND! (from first attempt)
         │
         ▼
     ON CONFLICT DO UPDATE
     (Update updated_at timestamp)
     ✗ NOT created again
         │
         ▼
     Result: ONLY ONE notification sent
             (never two!)

─────────────────────────────

Idempotency Key Format:
{notification_type}_{user_id}_{date}

Examples:
- 'reminder_user123_2025-12-21'
- 'budget_alert_user456_2025-12-21'
- 'weekly_summary_user789_2025-12-21'

Key Property:
If called again with same key
  → Idempotent (same result)
  → No duplicates
  → Safe to retry
```

---

**Version:** 1.0  
**Last Updated:** December 21, 2025  
**Status:** Complete ✅

Use these diagrams to understand the system architecture and data flow!
